{
  "name": "genshin-chrome",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "description": "A programmable Electron browser shell for debugging web applications.",
  "main": "src/main.ts",
  "imports": {
    "#/*": "./src/*"
  },
  "scripts": {
    "start": "bun run dev",
    "dev": "concurrently -k \"vite --host 127.0.0.1\" \"bun run dev:electron\"",
    "dev:electron": "wait-on tcp:5173 && cross-env VITE_DEV_SERVER_URL=http://127.0.0.1:5173 electron .",
    "build": "vite build",
    "preview": "electron .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --noEmit",
    "check": "bun run typecheck && bun run format:check && bun run test:smoke",
    "test:smoke": "bun run build && playwright test tests/smoke.spec.ts --workers=1"
  },
  "dependencies": {
    "vue": "3.5.41"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "@vitejs/plugin-vue-jsx": "5.1.6",
    "concurrently": "10.0.5",
    "cross-env": "10.1.0",
    "electron": "43.4.1",
    "prettier": "3.9.6",
    "typescript": "7.0.2",
    "vite": "8.2.1",
    "wait-on": "9.1.0"
  },
  "trustedDependencies": [
    "electron",
    "esbuild",
    "vue-demi"
  ]
}
